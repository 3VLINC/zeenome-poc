[package]
version = "0.1.0"
name = "zk-irisplex"
edition = "2021"
license = "MIT OR Apache-2.0"
publish = false

# Host package whose sole job is `build.rs` (emits the SP1 guest ELF).
# The POC proves via `client-cli`; no host demo binaries live here.

[lib]
path = "src/lib.rs"

[build-dependencies]
sp1-build.workspace = true
